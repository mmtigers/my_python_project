# Family Quest ラズパイデプロイ前 動作検証手順

Family Quest はラズパイ上で稼働させる前提のシステムだが、以下の手順に沿えば
ロジックの大部分は PC（ラズパイ以外の環境）で事前検証できる。

構成は以下の2階層。

- **バックエンド**: `MY_HOME_SYSTEM`（Python / FastAPI / SQLite）
- **フロントエンド**: `family-quest`（React / TypeScript / Vite）

---

## ① バックエンド (MY_HOME_SYSTEM) セットアップ

```bash
cd MY_HOME_SYSTEM
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt -r requirements-dev.txt
cp .env.example .env          # 必要な値を編集
```

## ② 自動テスト・静的解析

```bash
pytest                        # tests/ 配下35本超。DBは一時SQLiteに自動分離される
ruff check .                  # Lint
bandit -r .                   # セキュリティ静的解析
pip-audit                     # 依存脆弱性チェック
```

## ③ DB初期化 & バックエンド単体起動

```bash
python3 init_unified_db.py    # ローカルにSQLite DBを作成
python3 unified_server.py     # サーバー起動（ハードウェア非依存のAPIはこれで動く）
```

→ curl/ブラウザで `quest_router` 等のAPIを叩いて手動確認。

## ④ フロントエンド (family-quest) セットアップ・確認

```bash
cd ../family-quest
npm install
npm run lint
npm run dev                   # Vite開発サーバーでUI動作確認（バックエンドAPIをlocalhostに向ける）
npm run build                 # tsc -b && vite build で型チェック込みビルド確認
```

## ⑤ 統合確認（本番構成の再現）

```bash
# ③のビルド成果物を使い、.env の QUEST_DIST_DIR を family-quest/dist に設定
cd ../MY_HOME_SYSTEM
python3 unified_server.py     # フロント込みでラズパイと同じ配信構成をPC上で確認
```

## ⑥ 実機依存部分は割り切って確認

SwitchBot / Nature Remo / カメラ監視 / NASマウント(`/mnt/nas`) はモックがないため、
ここまでの手順ではエラーになるだけ。この部分だけはラズパイ実機に持って行った後、
`start_all.sh` 実行時のログ(`logs/`配下)で最終確認する。

---

## まとめ表

| 対象 | ラズパイ不要で検証可能 | 実機必須 |
|---|---|---|
| クエスト/レベル/DBロジック | ✅ pytest で網羅的に可能 | - |
| API・SPA配信の統合動作 | ✅ ローカルで再現可能 | - |
| SwitchBot/Nature Remo/カメラ/NASマウント | △ 起動はできてもエラーになる | ✅ 実機接続確認が必要 |

上記①〜⑤がすべて通ってから、ラズパイへ `git pull` ＋ `start_all.sh` でデプロイする。

---

## ⑦ Windows(PowerShell)での動作検証

### 初回セットアップ時の注意点(Pythonバージョン)

- **Python 3.12系を使うこと**。3.10だと `contourpy` 等の一部依存が入らず、3.14(最新)だと
  `numpy` 等のビルド済みwheelがまだ無く、ソースビルドしようとしてC11対応コンパイラ不足で失敗する。
- 複数バージョンが入っている場合は `py -3.12 -m venv .venv` のように明示的にバージョンを指定してvenvを作成する。
- PowerShellでのvenv有効化は `source .venv/bin/activate` ではなく `.venv\Scripts\Activate.ps1`。
  実行ポリシーで弾かれる場合は該当ウィンドウ限定で
  `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` を先に実行する。
- 初回起動時、DBが空だとフロント画面(家族選択・クエスト一覧)が何も表示されない。
  以下のシードAPIでマスターデータ(`quest_data.py`の内容)を投入すると解消する。
  ```powershell
  curl.exe -X POST http://localhost:8000/api/quest/seed
  ```
  (PowerShellの`curl`は`Invoke-WebRequest`のエイリアスで挙動が違うため`curl.exe`と明示するか、
  `Invoke-RestMethod -Method Post -Uri http://localhost:8000/api/quest/seed` を使う。)
- PowerShell 5.1系は `&&` でのコマンド連結に非対応。コマンドは1行ずつ実行する。
- `pip install -r requirements.txt -r requirements-dev.txt` が
  `UnicodeDecodeError: 'cp932' codec can't decode byte ...` で失敗する場合、
  requirementsファイル中の日本語コメントを日本語ロケール(cp932)としてデコードしようとして
  いるのが原因。ファイル先頭2行に `# -*- coding: utf-8 -*-` を追加すれば解消する
  (`requirements-dev.txt`は対応済み)。
- プロジェクトパスに日本語(例: `お家開発`, `Google ドライブ`)が含まれる環境では、
  `pip-audit` が `pip_api` 内部の `UnicodeDecodeError` で落ちることがある
  (サブプロセスで実行した `pip --version` の出力パスをUTF-8決め打ちでデコードしているため)。
  実行前に `$env:PYTHONUTF8 = "1"` をそのPowerShellウィンドウで一度設定すると解消する。

### 2回目以降の起動手順(`.venv`・DBが既にある場合)

**① バックエンド(PowerShellウィンドウ1)**
```powershell
cd MY_HOME_SYSTEM
.venv\Scripts\Activate.ps1
python unified_server.py
```
→ `Uvicorn running on http://0.0.0.0:8000` が出たらそのまま放置。
起動時に以下が出るが、いずれも無視してよい。

| メッセージ | 理由 |
|---|---|
| `GEMINI_API_KEYが設定されていません` | AI機能(Family Quest本体とは無関係)の警告 |
| `Quest App Directory NOT FOUND or NOT SET` | `family-quest/dist`(ビルド済みフロント)が無い警告。`npm run dev`で直接見る場合は無関係(⑤の統合確認をする時のみ関係) |
| `--- Logging error ---` (`PermissionError`, ログローテーション関連) | `logs/home_system.log`のローテーション時に他プロセス(Googleドライブ等の同期プロセスと推測)がファイルをロックしていて失敗。ログ書き込み自体は継続されるため動作に影響なし |
| `camera_monitor.py` の `max_workers must be greater than 0` | ハードウェア(カメラ)未接続による想定内のエラー(⑥参照) |

**② フロントエンド(PowerShellウィンドウ2、新規で開く)**
```powershell
cd family-quest
npm run dev
```
→ `Local: http://localhost:5173/quest/` が出たらそのまま放置。

**③ ブラウザで確認**
```
http://localhost:5173/quest/
```

前回投入したデータはDBに残っているので、シードAPIの再実行は不要。
終わったら両方のウィンドウで `Ctrl+C` で停止する。

### UI動作確認チェックリスト

2026-08の大改修で「ボス」「ギルド」「装備」「ファミリーマイレージ」「パーティ」
「週間ランキング」機能は廃止済み。以下の現行機能を中心に確認する。

| 画面/機能 | 確認内容 |
|---|---|
| レイアウト切替 | ブラウザ幅やDevToolsの端末シミュレーションで、横画面(4人常時表示の`FamilyDashboard`)⇔縦画面(タブ切替UI)が切り替わるか |
| ヘッダー | ユーザー切替、記録(年代記)画面への導線 |
| クエスト一覧 | 曜日・対象での絞り込み、完了操作、無限クエストの60秒クールダウンリング表示 |
| 承認待ち一覧(`ApprovalList`) | クエスト・アイテム使用申請の承認/拒否操作 |
| ごほうび(`RewardShop`) | 所持ゴールド表示、購入可否の見た目切り替え、購入操作 |
| もちもの(`InventoryList`) | アイテムの使用/キャンセル、楽観的UI更新(即時反映されるか) |
| レベルアップ演出 | `LevelUpModal`表示と効果音再生 |
| トースト通知 | レベルアップ等の通知が自動で消えるか(ブロッキングモーダルにならないか) |
| 設定画面(`SettingsModal`) | 表示密度・非識字モード・パネル色設定 → リロード後も保持されるか(`localStorage`) |
| アバターアップロード | 画像選択・プレビュー・アップロード成功/失敗表示 |
| 記録(`FamilyLog`) | 日付ごとのタイムライン表示、ステータス表示 |
| カメラ(`/camera`パス) | ライブ/録画タブ切替のUIは出るが、実機カメラ未接続のためエラー表示は想定内 |
| オフライン検知 | DevToolsでネットワークをOfflineにして挙動確認 |
