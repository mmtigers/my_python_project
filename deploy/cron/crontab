# ラズパイ一次ヘルスチェック (毎時10分。Issue #339 層1・#484/#492)
# home_system.serviceおよびそのプロセスツリー配下の監視(server_watchdog.py等)が
# 丸ごと停止した場合でも、cron駆動でサービスから独立したこのチェックだけは動き続ける。
# 毎時0分に走るnewface_monitor等の負荷と重ねないよう10分にずらしている。
10 * * * * /home/masahiro/develop/MY_HOME_SYSTEM/run_task.sh monitors/health_watch.py

# 週間ログ分析 (日曜 08:50)
50 8 * * 0 /home/masahiro/develop/MY_HOME_SYSTEM/run_task.sh monitors/log_analyzer.py

# 週間レポート(電気代・センサー集計の Discord 通知、月曜 08:30)
# weekly_analyze_report.py は「月曜の8時台」以外は no-op で、同日二重起動は
# FALLBACK_ROOT/last_weekly_report.txt の実行済みフラグ(#234)で抑止される。
# Issue #528: 以前はこのリポジトリ外の cron でのみ起動されていて復旧手順から漏れていた。
30 8 * * 1 /home/masahiro/develop/MY_HOME_SYSTEM/run_task.sh weekly_analyze_report.py

# データベースバックアップ (毎日 04:00)
# Note: 元コメントは日曜03:00でしたが、コードは毎日04:00でした。コードを優先しています。
0 4 * * * /home/masahiro/develop/MY_HOME_SYSTEM/run_task.sh /home/masahiro/develop/MY_HOME_SYSTEM/services/backup_service.py

# 録画(nvr_recordings)・HLS VOD キャッシュの保持期間削除は monitors/nas_monitor.py の
# run_retention_cleanup(scheduler_boot 経由・1日1回)が .env の RECORDING_RETENTION_DAYS /
# HLS_VOD_RETENTION_DAYS を参照して行う。以前ここにあった find -mtime +30 -delete は
# 設定値と無関係に30日で消してしまい(Issue #527)、削除件数も Discord サマリに計上されない
# ため撤去した。cron 側に同種の削除を追加しないこと。

# 2. 一括ダウンロード (毎日 2:00 に変更)
0 2 * * * /home/masahiro/develop/MY_HOME_SYSTEM/run_task.sh /home/masahiro/develop/DDD/batch_download_discord.py

0 3 * * * /usr/bin/rclone sync /home/masahiro/develop/docs gdrive:お家開発/docs > /dev/null 2>&1

# 毎日 09:15 に当日の 07:30～09:00 の entranceタイムラプスを生成
15 9 * * * cd /home/masahiro/develop/MY_HOME_SYSTEM && /home/masahiro/develop/MY_HOME_SYSTEM/.venv/bin/python monitors/daily_timelapse_job.py entrance --date $(date +\%Y-\%m-\%d) --start 07:30 --end 09:00

# 毎日 15:15 に当日の 13:00～15:00 の entranceタイムラプスを生成
15 15 * * * cd /home/masahiro/develop/MY_HOME_SYSTEM && /home/masahiro/develop/MY_HOME_SYSTEM/.venv/bin/python monitors/daily_timelapse_job.py entrance --date $(date +\%Y-\%m-\%d) --start 13:00 --end 15:00

# newface monitor per 1hour
# Issue #587: DDD/logs はリポジトリ管理外(.gitignore対象)で、他のcronエントリと
# 異なりrun_task.sh(起動のたびにMY_HOME_SYSTEM/logsをmkdir -pするstart_all.shに
# 依存)を経由しないため、このディレクトリを作成する手段がどこにも無かった。
# 存在しない場合はリダイレクト自体が失敗しPythonプロセスが一切起動しない
# (cronはシェルのリダイレクトエラーを検知できず無言で失敗する)。mkdir -pを追加する。
0 * * * * cd /home/masahiro/develop/DDD && mkdir -p /home/masahiro/develop/DDD/logs && /home/masahiro/develop/MY_HOME_SYSTEM/.venv/bin/python newface_monitor.py >> /home/masahiro/develop/DDD/logs/newface_monitor.log 2>&1

# Bluetoothスピーカー(Anker SoundCore 2)のキープアライブ — 2026-09-19 判断: 登録しない
#
# 経緯: #585 で「スクリプトがあるのに実行契機が皆無」の解消として
# tools/keep_alive_anker.sh を5分毎に登録し、#664 で keep_alive_speaker.sh を
# 廃止して本スクリプトへ一本化した。しかし 2026-09-19 の実機棚卸しで、
# config.ENABLE_BLUETOOTH が既定 False・実機 .env の SPEAKER_BLUETOOTH_MAC も
# 未設定であり、5分毎の実行が一度も鳴動せず no-op で正常終了し続けている
# (=BTスピーカー運用そのものが休止中)ことを確認した。ユーザー判断で
# 「休止で確定」とし、このエントリを外す。
#
# スクリプト(tools/keep_alive_anker.sh / tools/connect_speaker.sh)は削除しない。
# config.py の ENABLE_BLUETOOTH のコメントが再有効化手順として
# 「tools/connect_speaker.sh の定期実行の整備」を挙げており、休止の解除に必要な
# 資産だからである。運用を再開するときは以下を同時に行うこと:
#
#   1. .env に SPEAKER_BLUETOOTH_MAC を設定する
#   2. config.py の ENABLE_BLUETOOTH を True にする
#      (post_boot_health_check の Speaker チェックがBT確認に切り替わる)
#   3. OS 側で `sudo systemctl enable --now bluetooth`、実機に `sox` を導入する
#   4. 下記のエントリを復活させる(間隔は実機のオートパワーオフ時間に合わせる)
#      */5 * * * * /home/masahiro/develop/MY_HOME_SYSTEM/tools/keep_alive_anker.sh
#
# .github/scripts/test_crontab_keep_alive.py が「ENABLE_BLUETOOTH=False の間は
# このエントリを登録しない」という対応関係を検査する。
